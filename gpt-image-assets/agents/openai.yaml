interface:
  display_name: "gpt-image-assets"
  short_description: "通用 Agent Skill，用 GPT-Image-2 生成 PNG/PSD 素材。"
  default_prompt: "Use $gpt-image-assets to generate a PNG image or endpoint-supported layered PSD through official OpenAI or a third-party Responses-compatible proxy."

policy:
  allow_implicit_invocation: true
